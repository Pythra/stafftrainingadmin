name = "crunches-admin"
compatibility_date = "2024-09-23"

# Runs automatically before `wrangler deploy` (and `wrangler dev`)
[build]
command = "npm run build"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
